[project]
name = "sem5"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn",
    "catboost",
    "optuna",
    "ipykernel",
]
